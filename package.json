{
  "name": "chaitin-demo-console",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": { "node": ">=22.5" },
  "scripts": {
    "check": "node --check server.mjs && node --check demo-cases.mjs && node --check public/app.js && node --check release-runner/release-runner.mjs && node --check trigger-bridge/agent-trigger-bridge.mjs && node -e \"const fs=require('fs');const s=fs.readFileSync('release-runner/release-agent-project.sh','utf8');const e=fs.readFileSync('docker-entrypoint.sh','utf8');if(!s.startsWith('#!/bin/sh')||!s.includes('agent-compose up')||!e.includes('setpriv --reuid=node'))process.exit(1)\"",
    "test": "node --test test/*.test.mjs",
    "start": "node server.mjs"
  }
}
